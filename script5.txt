--[[\n    Script: Khang Nguyen Masterpiece Ultra v3.0 (Full Features & Stable UI)\n    Target Game: Steal an Egg (Roblox)\n    Author / Brand: Khang Nguyen\n    Line Count Standard: ~550+ Lines Optimized\n]]--

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:FindFirstChild("PlayerGui") or LocalPlayer:WaitForChild("PlayerGui")

getgenv().KhangNguyenConfig = getgenv().KhangNguyenConfig or {
    AntiRagdoll = true,
    SpeedBypass = false,
    SpeedValue = 60,
    AntiTrap = true,
    FixLag = true,
    Noclip = true,
    ShowPetInEgg = true,
    AutoGetEgg = false,
    SelectedEggFilter = "Secret",
    FlySpeed = 350,
    HitboxSize = 25,
    AutoAttack = false,
    TargetPlayer = "",
    AutoServerHop = false,
    MinPlayersHop = 3
}

print("==================================================")
print("[Khang Nguyen] Đang khởi chạy phiên bản Ultra 500+ dòng...")
print("==================================================")

-- Xóa UI cũ tránh xung đột
pcall(function()
    if CoreGui:FindFirstChild("KhangNguyenMasterUI") then CoreGui.KhangNguyenMasterUI:Destroy() end
    if PlayerGui:FindFirstChild("KhangNguyenMasterUI") then PlayerGui.KhangNguyenMasterUI:Destroy() end
end)

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "KhangNguyenMasterUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local uiSuccess = pcall(function() ScreenGui.Parent = CoreGui end)
if not uiSuccess then ScreenGui.Parent = PlayerGui end

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 480, 0, 400)
MainFrame.Position = UDim2.new(0.5, -240, 0.5, -200)
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
MainFrame.BorderColor3 = Color3.fromRGB(0, 255, 128)
MainFrame.BorderSizePixel = 2
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 8)
MainCorner.Parent = MainFrame

local TitleBar = Instance.new("TextLabel")
TitleBar.Size = UDim2.new(1, 0, 0, 45)
TitleBar.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
TitleBar.TextColor3 = Color3.fromRGB(0, 255, 128)
TitleBar.TextSize = 16
TitleBar.Text = "KHANG NGUYEN - STEAL AN EGG MASTER"
TitleBar.Font = Enum.Font.SourceSansBold
TitleBar.Parent = MainFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 8)
TitleCorner.Parent = TitleBar

local ScrollingFrame = Instance.new("ScrollingFrame")
ScrollingFrame.Size = UDim2.new(1, -20, 1, -55)
ScrollingFrame.Position = UDim2.new(0, 10, 0, 50)
ScrollingFrame.BackgroundTransparency = 1
ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, 750)
ScrollingFrame.ScrollBarThickness = 6
ScrollingFrame.Parent = MainFrame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.SortOrder = Enum.SortOrder.LayoutIndex
UIListLayout.Padding = UDim.new(0, 6)
UIListLayout.Parent = ScrollingFrame

local function CreateUISection(text)
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, 0, 0, 25)
    lbl.BackgroundTransparency = 1
    lbl.TextColor3 = Color3.fromRGB(200, 200, 200)
    lbl.TextSize = 13
    lbl.Font = Enum.Font.SourceSansBold
    lbl.Text = "--- " .. text .. " ---"
    lbl.Parent = ScrollingFrame
end

local function CreateUIToggle(name, configKey)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 35)
    btn.BackgroundColor3 = getgenv().KhangNguyenConfig[configKey] and Color3.fromRGB(0, 160, 80) or Color3.fromRGB(45, 45, 45)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 14
    btn.Font = Enum.Font.SourceSansBold
    btn.Text = name .. ": " .. (getgenv().KhangNguyenConfig[configKey] and "BẬT" | "TẮT")
    btn.Parent = ScrollingFrame

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn

    btn.MouseButton1Click:Connect(function()
        getgenv().KhangNguyenConfig[configKey] = not getgenv().KhangNguyenConfig[configKey]
        btn.BackgroundColor3 = getgenv().KhangNguyenConfig[configKey] and Color3.fromRGB(0, 160, 80) or Color3.fromRGB(45, 45, 45)
        btn.Text = name .. ": " .. (getgenv().KhangNguyenConfig[configKey] and "BẬT" | "TẮT")
    end)
end

CreateUISection("CHỨC NĂNG CƠ BẢN")
CreateUIToggle("Chống ngã/choáng (Anti-Ragdoll)", "AntiRagdoll")
CreateUIToggle("Đi xuyên tường (Noclip)", "Noclip")
CreateUIToggle("Vô hiệu hóa bẫy (Anti-Trap)", "AntiTrap")
CreateUIToggle("Tối ưu hóa game (Fix Lag)", "FixLag")

CreateUISection("TĂNG TỐC & DI CHUYỂN")
CreateUIToggle("Bật tốc độ chạy (Speed Bypass)", "SpeedBypass")

CreateUISection("HỆ THỐNG SĂN TRỨNG (AUTO FARM)")
CreateUIToggle("Tự động cướp trứng (Auto Farm Egg)", "AutoGetEgg")
CreateUIToggle("Hiển thị ESP Trứng & Pet", "ShowPetInEgg")

CreateUISection("PVP & SERVER HOP")
CreateUIToggle("Mở rộng Hitbox người chơi", "HitboxSizeEnabled")
CreateUIToggle("Tự động đánh mục tiêu (Auto Attack)", "AutoAttack")
CreateUIToggle("Tự động đổi Server ít người (Server Hop)", "AutoServerHop")

-- Xử lý Logic 1: Anti-Ragdoll
task.spawn(function()
    while task.wait(1) do
        if getgenv().KhangNguyenConfig.AntiRagdoll then
            pcall(function()
                local char = LocalPlayer.Character
                if char then
                    local humanoid = char:FindFirstChildOfClass("Humanoid")
                    if humanoid then
                        humanoid:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
                        humanoid:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
                        humanoid:SetStateEnabled(Enum.HumanoidStateType.PlatformStand, false)
                    end
                end
            end)
        end
    end
end)

-- Xử lý Logic 2: Speed Bypass
RunService.RenderStepped:Connect(function(dt)
    if getgenv().KhangNguyenConfig.SpeedBypass then
        pcall(function()
            local char = LocalPlayer.Character
            if char then
                local hrp = char:FindFirstChild("HumanoidRootPart")
                local humanoid = char:FindFirstChildOfClass("Humanoid")
                if hrp and humanoid then
                    local moveDir = humanoid.MoveDirection
                    if moveDir.Magnitude > 0 then
                        hrp.CFrame = hrp.CFrame + (moveDir * getgenv().KhangNguyenConfig.SpeedValue * dt)
                    end
                end
            end
        end)
    end
end)

-- Xử lý Logic 3: Noclip toàn diện
RunService.Stepped:Connect(function()
    if getgenv().KhangNguyenConfig.Noclip then
        pcall(function()
            local char = LocalPlayer.Character
            if char then
                for _, part in pairs(char:GetDescendants()) do
                    if part:IsA("BasePart") and part.CanCollide then
                        part.CanCollide = false
                    end
                end
            end
        end)
    end
end)

-- Xử lý Logic 4: Anti-Trap xóa bỏ bẫy
task.spawn(function()
    while task.wait(0.8) do
        if getgenv().KhangNguyenConfig.AntiTrap then
            pcall(function()
                for _, v in pairs(Workspace:GetDescendants()) do
                    if v and v.Parent and v:IsA("BasePart") then
                        local name = v.Name:lower()
                        if name:find("trap") or name:find("bẫy") or name:find("spike") or name:find("danger") then
                            v.CanCollide = false
                            v.Transparency = 1
                            local touch = v:FindFirstChild("TouchInterest")
                            if touch then touch:Destroy() end
                        end
                    end
                end
            end)
        end
    end
end)

-- Xử lý Logic 5: Fix Lag cực hạn
local function ApplyMasterFixLag()
    pcall(function()
        local terrain = Workspace:FindFirstChildOfClass("Terrain")
        if terrain then
            terrain.WaterWaveSize = 0
            terrain.WaterWaveTransparency = 1
        end
        for _, v in pairs(Workspace:GetDescendants()) do
            if v and v.Parent then
                if v:IsA("BasePart") then
                    v.Material = Enum.Material.SmoothPlastic
                    v.Reflectance = 0
                elseif v:IsA("Decal") or v:IsA("Texture") then
                    v.Transparency = 1
                elseif v:IsA("ParticleEmitter") or v:IsA("Fire") or v:IsA("Smoke") then
                    v.Enabled = false
                end
            end
        end
    end)
end
if getgenv().KhangNguyenConfig.FixLag then task.spawn(ApplyMasterFixLag) end

-- Xử lý Logic 6: ESP Trứng chi tiết
local EggCache = {}
task.spawn(function()
    while task.wait(1) do
        pcall(function()
            EggCache = {}
            for _, egg in pairs(Workspace:GetDescendants()) do
                if egg and egg.Parent and egg:IsA("Model") then
                    local nameLower = egg.Name:lower()
                    if nameLower:find("egg") or nameLower:find("trứng") then
                        local primaryPart = egg.PrimaryPart or egg:FindFirstChildWhichIsA("BasePart")
                        if primaryPart then
                            local data = {
                                Instance = egg,
                                Position = primaryPart.Position,
                                IsSecret = nameLower:find("secret") ~= nil
                            }
                            table.insert(EggCache, data)

                            if getgenv().KhangNguyenConfig.ShowPetInEgg and not egg:FindFirstChild("KhangNguyenESP") then
                                local hl = Instance.new("Highlight")
                                hl.Name = "KhangNguyenESP"
                                hl.Adornee = egg
                                hl.FillColor = data.IsSecret and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(0, 255, 255)
                                hl.OutlineColor = Color3.fromRGB(255, 255, 255)
                                hl.Parent = egg

                                local bb = Instance.new("BillboardGui")
                                bb.Name = "KhangNguyenTag"
                                bb.Size = UDim2.new(0, 120, 0, 50)
                                bb.AlwaysOnTop = true
                                bb.Adornee = primaryPart

                                local txt = Instance.new("TextLabel")
                                txt.Size = UDim2.new(1, 0, 1, 0)
                                txt.BackgroundTransparency = 1
                                txt.TextColor3 = Color3.fromRGB(255, 255, 255)
                                txt.TextScaled = true
                                txt.TextStrokeTransparency = 0
                                txt.Text = egg.Name .. "\n[Khang Nguyen]"
                                txt.Parent = bb
                                bb.Parent = egg
                            end
                        end
                    end
                end
            end
        end)
    end
end)

-- Xử lý Logic 7: Hàm bay mượt (Smooth Fly)
local function MasterFlyTo(targetCF)
    pcall(function()
        local char = LocalPlayer.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        
        local distance = (hrp.Position - targetCF.Position).Magnitude
        local speed = math.clamp(getgenv().KhangNguyenConfig.FlySpeed, 100, 800)
        local timeTaken = math.clamp(distance / speed, 0.05, 1.2)
        local startTime = tick()
        local startCF = hrp.CFrame
        
        while tick() - startTime < timeTaken do
            if not char or not hrp.Parent then break end
            local alpha = (tick() - startTime) / timeTaken
            hrp.CFrame = startCF:Lerp(targetCF, alpha)
            RunService.RenderStepped:Wait()
        end
        if hrp and hrp.Parent then hrp.CFrame = targetCF end
    end)
end

-- Xử lý Logic 8: Auto Farm Trứng thông minh
task.spawn(function()
    while task.wait(0.5) do
        if getgenv().KhangNguyenConfig.AutoGetEgg then
            pcall(function()
                local targetEgg = nil
                for _, egg in ipairs(EggCache) do
                    if egg.IsSecret then targetEgg = egg; break; end
                end
                if not targetEgg and #EggCache > 0 then
                    targetEgg = EggCache[math.random(1, #EggCache)]
                end

                if targetEgg and targetEgg.Instance and targetEgg.Instance.Parent then
                    local char = LocalPlayer.Character
                    if char then
                        local hrp = char:FindFirstChild("HumanoidRootPart")
                        if hrp then
                            local originCF = hrp.CFrame
                            MasterFlyTo(CFrame.new(targetEgg.Position + Vector3.new(0, 4, 0)))
                            task.wait(0.2)
                            local tool = char:FindFirstChildOfClass("Tool")
                            if tool then pcall(function() tool:Activate() end) end
                            task.wait(0.2)
                            MasterFlyTo(originCF)
                        end
                    end
                end
            end)
        end
    end
end)

-- Xử lý Logic 9: Hitbox mở rộng
RunService.RenderStepped:Connect(function()
    pcall(function()
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    hrp.Size = Vector3.new(getgenv().KhangNguyenConfig.HitboxSize, getgenv().KhangNguyenConfig.HitboxSize, getgenv().KhangNguyenConfig.HitboxSize)
                    hrp.Transparency = 0.8
                    hrp.CanCollide = false
                end
            end
        end
    end)
end)

-- Xử lý Logic 10: Auto Server Hop thông minh
task.spawn(function()
    while task.wait(20) do
        if getgenv().KhangNguyenConfig.AutoServerHop then
            pcall(function()
                local api = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"
                local success, result = pcall(function() return game:HttpGet(api) end)
                if success and result then
                    local decodeSuccess, response = pcall(function() return HttpService:JSONDecode(result) end)
                    if decodeSuccess and response and response.data then
                        for _, s in ipairs(response.data) do
                            if s.playing and s.maxPlayers and s.playing < s.maxPlayers and s.playing <= getgenv().KhangNguyenConfig.MinPlayersHop and s.id ~= game.JobId then
                                TeleportService:TeleportToPlaceInstance(game.PlaceId, s.id, LocalPlayer)
                                break
                            end
                        end
                    end
                end
            end)
        end
    end
end)

print("[Khang Nguyen] Tải thành công toàn bộ hệ thống Masterpiece Menu UI!")
